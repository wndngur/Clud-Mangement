# 🚀 알고리즘 성능 비교 차트 (n-s)

> **프로젝트**: Club Management System
> **목적**: 직접 구현 가능한 알고리즘들의 시간복잡도 비교
> **작성일**: 2025년 12월

---

## 📊 시간복잡도 비교 차트

### 1. 입력 크기(n)에 따른 연산 횟수

| 입력 크기 (n) | O(1) | O(log n) | O(n) | O(n log n) | O(n²) | O(2ⁿ) |
|--------------|------|----------|------|------------|-------|--------|
| **10개** | 1 | 3 | 10 | 33 | 100 | 1,024 |
| **50개** | 1 | 6 | 50 | 282 | 2,500 | 1.1×10¹⁵ |
| **100개** | 1 | 7 | 100 | 664 | 10,000 | 1.3×10³⁰ |
| **500개** | 1 | 9 | 500 | 4,483 | 250,000 | - |
| **1,000개** | 1 | 10 | 1,000 | 9,966 | 1,000,000 | - |
| **10,000개** | 1 | 13 | 10,000 | 132,877 | 100,000,000 | - |

### 2. 예상 실행 시간 (1 연산 = 1μs 가정)

| 입력 크기 (n) | O(1) | O(log n) | O(n) | O(n log n) | O(n²) |
|--------------|------|----------|------|------------|-------|
| **10개** | 1μs | 3μs | 10μs | 33μs | 100μs |
| **50개** | 1μs | 6μs | 50μs | 282μs | 2.5ms |
| **100개** | 1μs | 7μs | 100μs | 664μs | 10ms |
| **500개** | 1μs | 9μs | 500μs | 4.5ms | 250ms |
| **1,000개** | 1μs | 10μs | 1ms | 10ms | 1초 |
| **10,000개** | 1μs | 13μs | 10ms | 133ms | 100초 |

> **단위**: μs = 마이크로초, ms = 밀리초

---

## 🎯 정렬 알고리즘 비교

### 1. 정렬 알고리즘별 시간복잡도

| 알고리즘 | 최선 | 평균 | 최악 | 공간 복잡도 | 안정성 | 구현 난이도 |
|---------|------|------|------|------------|--------|-----------|
| **버블 정렬** | O(n) | O(n²) | O(n²) | O(1) | ✅ 안정 | ⭐ 쉬움 |
| **선택 정렬** | O(n²) | O(n²) | O(n²) | O(1) | ❌ 불안정 | ⭐ 쉬움 |
| **삽입 정렬** | O(n) | O(n²) | O(n²) | O(1) | ✅ 안정 | ⭐ 쉬움 |
| **퀵 정렬** | O(n log n) | O(n log n) | O(n²) | O(log n) | ❌ 불안정 | ⭐⭐⭐ 어려움 |
| **병합 정렬** | O(n log n) | O(n log n) | O(n log n) | O(n) | ✅ 안정 | ⭐⭐ 보통 |
| **힙 정렬** | O(n log n) | O(n log n) | O(n log n) | O(1) | ❌ 불안정 | ⭐⭐⭐ 어려움 |

### 2. 동아리 데이터 정렬 성능 예측

**시나리오**: 추천 점수로 동아리 정렬

| 동아리 개수 | 버블 정렬 (O(n²)) | 퀵 정렬 (O(n log n)) | 병합 정렬 (O(n log n)) |
|-----------|------------------|--------------------|--------------------|
| **10개** | 100μs (0.1ms) | 33μs | 33μs |
| **30개** | 900μs (0.9ms) | 147μs | 147μs |
| **50개** | 2.5ms | 282μs | 282μs |
| **100개** | 10ms | 664μs | 664μs |
| **500개** | 250ms ⚠️ | 4.5ms ✅ | 4.5ms ✅ |
| **1,000개** | 1초 ❌ | 10ms ✅ | 10ms ✅ |

> **권장**: 동아리가 50개 이상이면 O(n log n) 알고리즘 사용

---

## 🔍 검색 알고리즘 비교

### 1. 검색 알고리즘별 시간복잡도

| 알고리즘 | 시간 복잡도 | 전제 조건 | 사용 사례 |
|---------|-----------|-----------|----------|
| **선형 검색** | O(n) | 없음 | 정렬되지 않은 데이터 |
| **이진 검색** | O(log n) | 정렬된 데이터 | 정렬된 배열/리스트 |
| **해시 검색** | O(1) 평균 | 해시 테이블 | Key-Value 매핑 |

### 2. 회원 검색 성능 비교

| 회원 수 | 선형 검색 (O(n)) | 이진 검색 (O(log n)) | 해시 검색 (O(1)) |
|--------|----------------|--------------------|--------------------|
| **10명** | 10μs | 3μs | 1μs |
| **50명** | 50μs | 6μs | 1μs |
| **100명** | 100μs | 7μs | 1μs |
| **500명** | 500μs | 9μs | 1μs |
| **1,000명** | 1ms | 10μs | 1μs |
| **10,000명** | 10ms | 13μs | 1μs |

---

## 💻 직접 구현 알고리즘 코드

### 1. 버블 정렬 (현재 프로젝트 사용 중)

```java
/**
 * 버블 정렬 - 추천 점수 기준 내림차순
 * 시간 복잡도: O(n²)
 * 공간 복잡도: O(1)
 */
public void bubbleSort(List<Club> clubs, List<Integer> scores) {
    int n = scores.size();

    for (int i = 0; i < n - 1; i++) {
        for (int j = 0; j < n - i - 1; j++) {
            // 내림차순: 앞이 뒤보다 작으면 교환
            if (scores.get(j) < scores.get(j + 1)) {
                // 점수 교환
                int tempScore = scores.get(j);
                scores.set(j, scores.get(j + 1));
                scores.set(j + 1, tempScore);

                // 동아리 교환
                Club tempClub = clubs.get(j);
                clubs.set(j, clubs.get(j + 1));
                clubs.set(j + 1, tempClub);
            }
        }
    }
}
```

**입력 크기별 실제 비교 횟수:**
- n=10: 45회
- n=50: 1,225회
- n=100: 4,950회

---

### 2. 최적화된 버블 정렬

```java
/**
 * 최적화된 버블 정렬 - 조기 종료
 * 최선 시간 복잡도: O(n) - 이미 정렬된 경우
 * 평균/최악: O(n²)
 */
public void optimizedBubbleSort(List<Club> clubs, List<Integer> scores) {
    int n = scores.size();
    boolean swapped;

    for (int i = 0; i < n - 1; i++) {
        swapped = false;

        for (int j = 0; j < n - i - 1; j++) {
            if (scores.get(j) < scores.get(j + 1)) {
                // 교환 로직
                swap(scores, j, j + 1);
                swap(clubs, j, j + 1);
                swapped = true;
            }
        }

        // 교환이 없으면 이미 정렬됨 → 종료
        if (!swapped) break;
    }
}
```

**장점:**
- 이미 정렬된 데이터에서 O(n)으로 개선
- 부분적으로 정렬된 데이터에서 빠름

---

### 3. 선택 정렬 (대안)

```java
/**
 * 선택 정렬 - 가장 큰 값을 찾아 앞으로 이동
 * 시간 복잡도: O(n²) (항상)
 * 공간 복잡도: O(1)
 */
public void selectionSort(List<Club> clubs, List<Integer> scores) {
    int n = scores.size();

    for (int i = 0; i < n - 1; i++) {
        int maxIdx = i;

        // 최댓값 찾기
        for (int j = i + 1; j < n; j++) {
            if (scores.get(j) > scores.get(maxIdx)) {
                maxIdx = j;
            }
        }

        // 최댓값을 현재 위치로 이동
        if (maxIdx != i) {
            swap(scores, i, maxIdx);
            swap(clubs, i, maxIdx);
        }
    }
}
```

**특징:**
- 비교 횟수: n(n-1)/2 (버블 정렬과 동일)
- 교환 횟수: 최대 n-1회 (버블 정렬보다 적음)
- **추천**: 교환 비용이 비싼 경우 유리

---

### 4. 삽입 정렬 (추천)

```java
/**
 * 삽입 정렬 - 정렬된 부분에 삽입
 * 최선 시간 복잡도: O(n) - 이미 정렬됨
 * 평균/최악: O(n²)
 * 공간 복잡도: O(1)
 */
public void insertionSort(List<Club> clubs, List<Integer> scores) {
    int n = scores.size();

    for (int i = 1; i < n; i++) {
        int keyScore = scores.get(i);
        Club keyClub = clubs.get(i);
        int j = i - 1;

        // keyScore보다 작은 요소들을 뒤로 이동
        while (j >= 0 && scores.get(j) < keyScore) {
            scores.set(j + 1, scores.get(j));
            clubs.set(j + 1, clubs.get(j));
            j--;
        }

        // key 삽입
        scores.set(j + 1, keyScore);
        clubs.set(j + 1, keyClub);
    }
}
```

**장점:**
- 작은 데이터셋에서 매우 빠름 (n < 50)
- 거의 정렬된 데이터에서 O(n)
- 온라인 알고리즘 (실시간 정렬 가능)
- **추천**: 동아리가 30개 이하일 때 최적

---

### 5. 퀵 정렬 (고급)

```java
/**
 * 퀵 정렬 - 분할 정복
 * 평균 시간 복잡도: O(n log n)
 * 최악: O(n²) - 이미 정렬된 경우
 * 공간 복잡도: O(log n)
 */
public void quickSort(List<Club> clubs, List<Integer> scores, int low, int high) {
    if (low < high) {
        int pivotIdx = partition(clubs, scores, low, high);

        quickSort(clubs, scores, low, pivotIdx - 1);
        quickSort(clubs, scores, pivotIdx + 1, high);
    }
}

private int partition(List<Club> clubs, List<Integer> scores, int low, int high) {
    int pivot = scores.get(high);
    int i = low - 1;

    for (int j = low; j < high; j++) {
        // 내림차순: pivot보다 크면 왼쪽으로
        if (scores.get(j) > pivot) {
            i++;
            swap(scores, i, j);
            swap(clubs, i, j);
        }
    }

    swap(scores, i + 1, high);
    swap(clubs, i + 1, high);
    return i + 1;
}

// 사용 예시
quickSort(clubs, scores, 0, scores.size() - 1);
```

**최적화 - 랜덤 피벗 선택:**
```java
private int randomizedPartition(List<Club> clubs, List<Integer> scores,
                                 int low, int high) {
    Random rand = new Random();
    int randomIdx = low + rand.nextInt(high - low + 1);

    // 랜덤 원소를 마지막으로 이동
    swap(scores, randomIdx, high);
    swap(clubs, randomIdx, high);

    return partition(clubs, scores, low, high);
}
```

**장점:**
- 평균적으로 가장 빠름
- 캐시 효율이 좋음
- **추천**: 동아리가 100개 이상일 때

---

### 6. 병합 정렬 (안정적)

```java
/**
 * 병합 정렬 - 분할 정복 + 안정 정렬
 * 시간 복잡도: O(n log n) (항상)
 * 공간 복잡도: O(n)
 */
public void mergeSort(List<Club> clubs, List<Integer> scores, int left, int right) {
    if (left < right) {
        int mid = left + (right - left) / 2;

        mergeSort(clubs, scores, left, mid);
        mergeSort(clubs, scores, mid + 1, right);
        merge(clubs, scores, left, mid, right);
    }
}

private void merge(List<Club> clubs, List<Integer> scores,
                   int left, int mid, int right) {
    // 임시 배열 생성
    List<Integer> tempScores = new ArrayList<>();
    List<Club> tempClubs = new ArrayList<>();

    int i = left, j = mid + 1;

    // 병합
    while (i <= mid && j <= right) {
        if (scores.get(i) >= scores.get(j)) {  // 내림차순
            tempScores.add(scores.get(i));
            tempClubs.add(clubs.get(i));
            i++;
        } else {
            tempScores.add(scores.get(j));
            tempClubs.add(clubs.get(j));
            j++;
        }
    }

    // 남은 원소 추가
    while (i <= mid) {
        tempScores.add(scores.get(i));
        tempClubs.add(clubs.get(i));
        i++;
    }

    while (j <= right) {
        tempScores.add(scores.get(j));
        tempClubs.add(clubs.get(j));
        j++;
    }

    // 원본 배열에 복사
    for (int k = 0; k < tempScores.size(); k++) {
        scores.set(left + k, tempScores.get(k));
        clubs.set(left + k, tempClubs.get(k));
    }
}

// 사용 예시
mergeSort(clubs, scores, 0, scores.size() - 1);
```

**장점:**
- 항상 O(n log n) 보장
- 안정 정렬
- 예측 가능한 성능
- **추천**: 정렬 안정성이 중요할 때

---

### 7. 이진 검색

```java
/**
 * 이진 검색 - 정렬된 배열에서 검색
 * 시간 복잡도: O(log n)
 * 전제 조건: 배열이 정렬되어 있어야 함
 */
public int binarySearch(List<String> sortedNames, String target) {
    int left = 0;
    int right = sortedNames.size() - 1;

    while (left <= right) {
        int mid = left + (right - left) / 2;
        int cmp = sortedNames.get(mid).compareTo(target);

        if (cmp == 0) {
            return mid;  // 찾음
        } else if (cmp < 0) {
            left = mid + 1;  // 오른쪽 검색
        } else {
            right = mid - 1;  // 왼쪽 검색
        }
    }

    return -1;  // 못 찾음
}

// 재귀 버전
public int binarySearchRecursive(List<String> sortedNames, String target,
                                  int left, int right) {
    if (left > right) return -1;

    int mid = left + (right - left) / 2;
    int cmp = sortedNames.get(mid).compareTo(target);

    if (cmp == 0) return mid;
    else if (cmp < 0) return binarySearchRecursive(sortedNames, target, mid + 1, right);
    else return binarySearchRecursive(sortedNames, target, left, mid - 1);
}
```

**비교 횟수:**
- n=10: 최대 4회
- n=100: 최대 7회
- n=1,000: 최대 10회
- n=10,000: 최대 14회

---

## 🎯 알고리즘 선택 가이드

### 1. 정렬 알고리즘 선택 기준

```
데이터 크기(n)에 따른 추천:

n ≤ 10개
  └─> 버블 정렬 / 삽입 정렬
      (구현 간단, 충분히 빠름)

10 < n ≤ 50개
  └─> 삽입 정렬
      (작은 데이터에서 가장 빠름)

50 < n ≤ 100개
  └─> 버블 정렬 (최적화 버전)
  └─> 퀵 정렬 (더 빠름)

n > 100개
  └─> 퀵 정렬 (평균적으로 빠름)
  └─> 병합 정렬 (안정성 필요 시)
  └─> Java Collections.sort() 사용 권장
```

### 2. 프로젝트별 추천

#### Club Management System (현재 프로젝트)

| 기능 | 데이터 크기 | 추천 알고리즘 | 이유 |
|-----|-----------|--------------|------|
| **동아리 추천 정렬** | 10~50개 | 삽입 정렬 | 작은 데이터, 실시간 정렬 |
| **회원 목록 정렬** | 10~100명 | 퀵 정렬 | 중간 크기, 빠른 정렬 |
| **공지사항 정렬** | 10~200개 | 병합 정렬 | 안정 정렬 필요 |
| **회원 검색** | 10~1,000명 | 이진 검색 | 정렬 후 빠른 검색 |

---

## 📈 성능 테스트 코드

### 1. 정렬 알고리즘 성능 측정

```java
public class SortingBenchmark {

    public static void measureSortingTime(List<Club> clubs, List<Integer> scores,
                                           String algorithmName) {
        // 복사본 생성 (원본 보존)
        List<Club> testClubs = new ArrayList<>(clubs);
        List<Integer> testScores = new ArrayList<>(scores);

        // 시작 시간
        long startTime = System.nanoTime();

        // 정렬 실행
        switch (algorithmName) {
            case "bubble":
                bubbleSort(testClubs, testScores);
                break;
            case "insertion":
                insertionSort(testClubs, testScores);
                break;
            case "quick":
                quickSort(testClubs, testScores, 0, testScores.size() - 1);
                break;
            case "merge":
                mergeSort(testClubs, testScores, 0, testScores.size() - 1);
                break;
        }

        // 종료 시간
        long endTime = System.nanoTime();
        long duration = (endTime - startTime) / 1000; // 마이크로초 변환

        System.out.println(algorithmName + " 정렬: " + duration + "μs");
    }

    // 사용 예시
    public static void main(String[] args) {
        List<Club> clubs = generateRandomClubs(100);
        List<Integer> scores = generateRandomScores(100);

        System.out.println("=== 100개 동아리 정렬 성능 ===");
        measureSortingTime(clubs, scores, "bubble");
        measureSortingTime(clubs, scores, "insertion");
        measureSortingTime(clubs, scores, "quick");
        measureSortingTime(clubs, scores, "merge");
    }
}
```

**예상 출력:**
```
=== 100개 동아리 정렬 성능 ===
bubble 정렬: 8,542μs
insertion 정렬: 4,231μs
quick 정렬: 673μs
merge 정렬: 712μs
```

---

## 🔧 최적화 팁

### 1. 정렬 최적화

```java
// ❌ 나쁜 예: 매번 정렬
public void updateRecommendations() {
    List<Club> clubs = getClubs();
    sortByScore(clubs);  // O(n²)
    displayClubs(clubs);
}

// ✅ 좋은 예: 캐싱 + 필요 시에만 정렬
private List<Club> cachedSortedClubs;
private boolean needsResort = true;

public void updateRecommendations() {
    if (needsResort) {
        cachedSortedClubs = getClubs();
        sortByScore(cachedSortedClubs);
        needsResort = false;
    }
    displayClubs(cachedSortedClubs);
}
```

### 2. Java 내장 정렬 활용

```java
// 가장 효율적인 방법
Collections.sort(clubs, (c1, c2) -> {
    int score1 = c1.calculateRecommendScore(...);
    int score2 = c2.calculateRecommendScore(...);
    return Integer.compare(score2, score1);  // 내림차순
});

// 또는 Comparator 구현
clubs.sort(Comparator.comparingInt(Club::getScore).reversed());
```

**장점:**
- 최적화된 Dual-Pivot QuickSort 사용
- 평균 O(n log n)
- 안정 정렬 옵션 제공

---

## 📊 복잡도 빅오 표기법 정리

### 성능 순서 (빠름 → 느림)

```
O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ) < O(n!)
```

### 시각적 비교

```
n = 1,000일 때:

O(1):        1 연산          ████
O(log n):    10 연산         ████████
O(n):        1,000 연산      ████████████████
O(n log n):  10,000 연산     ████████████████████
O(n²):       1,000,000 연산  ████████████████████████████████
```

---

## 🎓 학습 순서 추천

### 초급 (쉬움)
1. ✅ 버블 정렬
2. ✅ 선택 정렬
3. ✅ 삽입 정렬
4. ✅ 선형 검색

### 중급 (보통)
5. ⭐ 병합 정렬
6. ⭐ 이진 검색
7. ⭐ 힙 정렬

### 고급 (어려움)
8. 🔥 퀵 정렬
9. 🔥 해시 테이블
10. 🔥 동적 계획법

---

## 📚 참고 자료

1. **책**
   - Introduction to Algorithms (CLRS)
   - Algorithms (Robert Sedgewick)

2. **웹사이트**
   - VisuAlgo (알고리즘 시각화)
   - Big-O Cheat Sheet

3. **도구**
   - Android Studio Profiler
   - Java VisualVM

---

## 📝 요약

### 핵심 포인트

1. **작은 데이터 (n < 50)**: 간단한 O(n²) 알고리즘 사용
2. **중간 데이터 (50 ≤ n ≤ 100)**: 삽입 정렬 또는 퀵 정렬
3. **큰 데이터 (n > 100)**: 퀵/병합 정렬 또는 Java 내장 정렬

### 프로젝트 적용

현재 Club Management System:
- ✅ 버블 정렬 (10~50개 동아리) → 적절
- 💡 100개 이상으로 확장 시 → 퀵 정렬로 변경 권장

---

**작성자**: [학번] [이름]
**제출일**: 2025년 12월
